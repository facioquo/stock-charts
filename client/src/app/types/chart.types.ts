/**
 * Alias for backward-compatible `UserSettings` references.
 * `ChartSettings` is the canonical name in `@facioquo/indy-charts`.
 */
export type { ChartSettings as UserSettings } from "@facioquo/indy-charts";
