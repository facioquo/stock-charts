# pnpm configuration
# Use hoisted node_modules for Angular and Jest compatibility
node-linker=hoisted

# Enable shamefully hoisting for packages that don't properly declare peer dependencies
shamefully-hoist=true

# Strict peer dependencies (warn but don't fail)
strict-peer-dependencies=false
